generate:
  checkpoint_path: ${artifacts_dir}/test/checkpoints/checkpoint-step-9500
  config_path: ${artifacts_dir}/test/config.yaml
  
  enable_ttt: false
  use_data_augmentation_for_generation: false
  num_augmentations: 10
  batch_size_generation: 5
  grid_select_policy: naive # naive, grid-wise, cell-wise-argmax
