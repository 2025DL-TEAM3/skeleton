generate:
  checkpoint_path: ${artifacts_dir}/checkpoint-final
  config_path: ${artifacts_dir}/config.yaml
  
  enable_ttt: false
  use_data_augmentation_for_generation: true
  num_augmentations: 8
  grid_select_policy: naive # naive, grid-wise, cell-wise-argmax
